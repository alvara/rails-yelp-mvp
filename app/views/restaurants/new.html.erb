<h1>Let's Create a New Restaurant</h1>
<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :category, collection: Restaurant::CATEGORIES %>
  <%= f.submit 'Create New Restaurant', class: 'btn btn-primary' %>
<% end %>

